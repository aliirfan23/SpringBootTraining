spring:
  profiles:
    active: local
  h2:
    console:
      enabled: true
  datasource:
    url: ${Db.url}
    username: ${Db.username}
    password: ${Db.password}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 1010758513764-fs56uu4v0ov9m5lh7cr1m9b2jle5qn8s.apps.googleusercontent.com
            client-secret: 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
            scope:
              - openid
              - profile
              - email
        provider:
          google:
            authorization-uri: https://accounts.google.com/o/oauth2/v2/auth
            token-uri: https://oauth2.googleapis.com/token
            user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo
            user-name-attribute: email

logging:
  level:
    sql: debug

jwt:
  signing:
    key: 12345678900123456789012345678912

management:
  endpoints:
    web:
      exposure:
        include: "*"
